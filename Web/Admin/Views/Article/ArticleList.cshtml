@{
    Layout = null;
}
<div class="panel panel-default">
    <div class="panel-heading">
        <h4 class="panel-title" ng-bind="title"></h4>
    </div>
    <div class="panel-body">
        <div class="pull-left">

        </div>
        <div class="pull-right padding-right100">

        </div>
    </div>
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>#</th>
                <th>ID</th>
                <th>标题</th>
                <th>作者ID</th>
                <th>分类</th>
                <th>置顶</th>
                <th>创建时间</th>
            </tr>
        </thead>
        <tbody ng-show="articles">
            <tr ng-repeat="article in articles">
                <td><input type="checkbox" ng-model="article.Checked" ng-change="checkedChange(article)"></td>
                <td><span ng-bind="article.ID"></span></td>
                <td><a ng-href="/#/articlechange/{{article.ID}}">{{article.Title}}</a></td>
                <td ng-bind="article.CreateBy"></td>
                <td ng-bind="article.CNCategory"></td>                
                <td ng-bind="article.IsTop?'置顶':'普通'"></td>
                <td ng-bind="article.CreateAt*1000 | date:'yyyy-MM-dd HH:mm:ss'"></td>
            </tr>
        </tbody>
    </table>
    <pagination total-items="totalCount" page="currentPage" first-text="首页" on-select-page="pageChange(page)" next-text="下一页" previous-text="上一页" last-text="尾页" max-size="10" class="pagination-sm" boundary-links="true" rotate="false" num-pages="numPages"></pagination>
    <pre>共{{totalCount}}条数据  第{{currentPage}}页 /共{{numPages}}页</pre>
</div>